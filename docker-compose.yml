services:
  etna-app:
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    container_name: etna-app-container
    volumes:
      - ./results:/app/results
      - ./data:/app/data
      - .:/app 
    environment:
      - PYTHONUNBUFFERED=1
    restart: on-failure